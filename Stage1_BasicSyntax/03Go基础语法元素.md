# Go 语言基础语法元素笔记

## 一、 25 个关键字 (Keywords)

关键字是 Go 语言的“保留词”，**不能**用来做变量名或函数名。Go 的关键字非常少（只有 25 个），这体现了它“少即是多”的设计哲学。

我们可以把它们分为 **4 大类** 来记忆：

### 1. 声明与定义 (4个)

这些词用来告诉编译器：“我要造一个新的东西了”。

| **关键字**  | **含义** | **备注**                                            |
| ----------- | -------- | --------------------------------------------------- |
| **package** | 包声明   | 每个文件的第一行必须是它，定义代码所属的包。        |
| **import**  | 引入包   | 引入别的包（比如 `fmt`）来使用其中的功能。          |
| **func**    | 函数定义 | 定义一个函数（Function）或方法。                    |
| **type**    | 类型定义 | 定义新的结构体(`struct`)、接口(`interface`)或别名。 |

### 2. 变量与常量 (4个)

| **关键字** | **含义**  | **备注**                                             |
| ---------- | --------- | ---------------------------------------------------- |
| **var**    | 变量声明  | `var x int`，声明一个变量。                          |
| **const**  | 常量声明  | `const PI = 3.14`，定义后不可修改。                  |
| **map**    | 字典/映射 | 引用类型，类似于 Python 的 dict 或 Java 的 HashMap。 |
| **struct** | 结构体    | 值类型，Go 语言中类的替代品，用于聚合数据。          |

### 3. 程序流程控制 (12个)

这些词控制代码执行的顺序（判断、循环、跳转）。

| **关键字**                        | **含义** | **核心点**                                                   |
| --------------------------------- | -------- | ------------------------------------------------------------ |
| **if**, **else**                  | 条件判断 | 这里的条件表达式**不需要加括号** `()`。                      |
| **for**                           | 循环     | Go 只有 `for`，**没有 `while`**。                            |
| **switch**, **case**, **default** | 多路选择 | 用于替代复杂的 if-else。                                     |
| **break**                         | 跳出     | 跳出循环或 switch。                                          |
| **continue**                      | 继续     | 跳过本次循环，进入下一次。                                   |
| **goto**                          | 跳转     | 直接跳转到指定标签（不推荐滥用）。                           |
| **fallthrough**                   | 穿透     | **Go 特有**。强制执行 switch 中的下一个 case（Go 的 switch 默认自动 break，想穿透必须显式写）。 |
| **range**                         | 范围遍历 | 配合 `for` 使用，用于遍历数组、切片、Map、字符串。           |
| **return**                        | 返回     | 从函数返回结果。                                             |
| **defer**                         | 延迟执行 | **Go 特有**。函数结束前才执行，常用于关闭文件、释放锁（类似 finally）。 |

### 4. 并发编程 (3个) —— **Go 的灵魂**

| **关键字** | **含义** | **备注**                                                     |
| ---------- | -------- | ------------------------------------------------------------ |
| **go**     | 启动协程 | `go func()`，极简地开启一个并发任务。                        |
| **chan**   | 通道     | Channel，用于协程之间的数据通信（不要通过共享内存来通信，而要通过通信来共享内存）。 |
| **select** | 多路复用 | 专门用于监听多个 Channel 的读写状态，类似 switch，但针对的是 IO 操作。 |

### 5. 接口 (2个)

| **关键字**    | **含义** | **备注**                     |
| ------------- | -------- | ---------------------------- |
| **interface** | 接口     | 定义一组方法签名，实现多态。 |

------

## 二、 36 个预定义标识符 (Predefined Identifiers)

这些不是关键字，而是 Go 提前定义好的**名字**。

- **区别**：理论上你可以用它们做变量名（比如定义 `var true = 1`），但**千万别这么干**，会被打死的。
- **分类**：它们主要是内置的**类型**、**常量**和**函数**。

### 1. 基础数据类型 (Types)

- **布尔**: `bool` (真/假)
- **整型**:
  - `int`, `int8`, `int16`, `int32`, `int64` (数字代表位数)
  - `uint`, `uint8`, `uint16`, `uint32`, `uint64` (u 代表 unsigned，无符号/非负数)
  - `uintptr` (存放指针地址的整数类型)
  - `byte` (其实就是 `uint8` 的别名，常用于处理原始数据)
- **浮点型**: `float32`, `float64` (小数)
- **复数**: `complex64`, `complex128` (数学上的复数，平时很少用)
- **字符串**: `string`
- **错误**: `error` (Go 处理错误的核心接口)

### 2. 内置常量 (Constants)

- `true`, `false`: 布尔值的真和假。
- `nil`: **空值**。相当于 Java 的 `null` 或 Python 的 `None`。它是指针、切片、Map、接口的“零值”。
- `iota`: **计数器**。Go 特有的枚举生成器，用于在 `const` 块中自动生成 0, 1, 2, 3...

### 3. 内置函数 (Functions)

不需要 `import` 任何包就能直接用的“出厂自带”工具。

| **函数**                        | **用途** | **常见场景**                                               |
| ------------------------------- | -------- | ---------------------------------------------------------- |
| **make**                        | 创建     | 专门用于创建 **Slice, Map, Channel**（分配内存并初始化）。 |
| **new**                         | 创建     | 用于分配内存，返回指针（用得比 make 少）。                 |
| **len**                         | 长度     | 获取字符串、数组、Slice、Map、Channel 的长度。             |
| **cap**                         | 容量     | 获取 Slice 或 Channel 的容量。                             |
| **append**                      | 追加     | 向 Slice（切片）追加元素，会自动扩容。                     |
| **copy**                        | 复制     | 复制 Slice。                                               |
| **close**                       | 关闭     | 关闭 Channel。                                             |
| **delete**                      | 删除     | 删除 Map 中的某个 Key。                                    |
| **panic**, **recover**          | 异常处理 | 类似于 `throw` 和 `try-catch`，用于处理严重崩溃。          |
| **print**, **println**          | 打印     | **调试用**。正式代码请用 `fmt.Println`。                   |
| **complex**, **real**, **imag** | 复数运算 | 处理复数的实部和虚部。                                     |

------

## 三、 特殊符号与分隔符

Go 语言对符号的使用非常克制，每个符号都有严格的语义。

### 1. 括号三兄弟

- **`()` 圆括号**：
  - 函数参数列表：`func main()`
  - 改变运算优先级：`(1 + 2) * 3`
- **`[]` 中括号**：
  - 数组/切片定义：`[]int`
  - 索引取值：`arr[0]`
- **`{}` 大括号**：
  - **代码块**：Go 语言非常依赖大括号来界定作用域。
  - **强制风格**：Go 强制要求左大括号 `{` **必须**放在行尾，不能换行写（否则报错）。

### 2. 标点符号

- **`.` (点)**：调用方法或属性。例如 `fmt.Println`（调用 fmt 包里的 Println）。
- **`,` (逗号)**：分隔参数。
- **`;` (分号)**：语句结束符。
  - **注意**：Go 的编译器会在每行代码末尾**自动加分号**，所以你平时写代码**不需要**手动写分号（除非你想把多条语句写在同一行）。
- **`:` (冒号)**：常用在切片操作 `arr[1:3]` 或结构体键值对。
- **`...` (省略号)**：
  - 变长参数：`func sum(nums ...int)` 表示可以传任意个 int。
  - 解包：将切片打散传入函数。

------

### 💡 补充（重点笔记）

在初学阶段，你最容易混淆或者需要特别注意的几个点：

1. **`make` vs `new`**:

   - 虽然都是分配内存，但 `make` 只用于三种“引用类型”（Slice, Map, Channel），而且返回的不是指针。
   - `new` 用于其他类型，返回的是指针。

2. **`defer` 是神技**:

   - 在打开文件后立马写 `defer file.Close()`，这样不管函数怎么运行、怎么报错，最后都会自动关文件，防泄漏神器。

3. **大括号 `{` 不能换行**:

   - 这是 Go 最大的“强迫症”。

   Go

   ```
   // 正确
   if a > 1 {
   }
   
   // 错误 (编译不通过)
   if a > 1
   {
   }
   ```